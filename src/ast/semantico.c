#include "ast.h"

void avaliar_programa(AST *nodo) {
  //   if (nodo) {
  //     percorrer(nodo, lambda(void, (AST * no) { return; }));
  //   }
}

void avaliar_funcao(AST *nodo) {
  if (nodo) {
    // percorrer(nodo, lambda(void, (AST * no), {

    //                              }));
  }
}